{
    "scripts": {
        "dev": "ts-node-dev --respawn --transpile-only src/"
    },
    "devDependencies": {
        "ts-node-dev": "^1.1.8",
        "typescript": "^4.5.5"
    },
    "dependencies": {
        "apollo-server": "^3.6.2",
        "graphql": "^16.3.0"
    }
}
